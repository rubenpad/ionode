'use strict'

const testServer = require('../utils/testServer')

describe('routes api tests', () => {
  let request = null

  beforeEach(() => {
    request = testServer()
  })
  test('GET agents', () => {})
})
